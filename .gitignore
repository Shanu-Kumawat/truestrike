node_modules/
dist/
coverage/
*.log

# Secrets - never commit
.env
.env.local

# Local AI-agent working memory (per-machine, not shared via git)
.agents/memory/

# TrueStrike runtime artifacts (gateway audit log, run state)
.truestrike/
.wrangler/

# Scan report outputs (per-session pentest reports)
truestrike-runs/
